---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dummyresources.dummy.jupyter.org
spec:
  group: dummy.jupyter.org
  names:
    kind: DummyResource
    listKind: DummyResourceList
    plural: dummyresources
    singular: dummyresource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DummyResource represents a dummy resource for demonstrating InstallAPIGroup
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DummyResourceSpec defines the desired state of DummyResource
            properties:
              message:
                description: Message is a simple string field
                type: string
            required:
            - message
            type: object
          status:
            description: DummyResourceStatus defines the observed state of DummyResource
            properties:
              lastUpdate:
                description: LastUpdate is the timestamp of the last update
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the resource
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
